#include<stdio.h>
void length( char a[]);
int main()
{
    char a[20] ="BHOPAL";
    
    // printf("Enter string :");
    // scanf("%s",str);
    length(a[]);
    return 0;

}
void length( char a[])
{
    int i;
    for(i=0;i<=6;i++);
    printf("length of the given string is %d",i);

}